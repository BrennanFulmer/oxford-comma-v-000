def oxford_comma(array)
  sized = array.size()
  if sized === 1
    array.join()
  elsif sized === 2
    "#{array[0]} and #{array[1]}"
  elsif sized === 3
    "#{array[0]}, #{array[1]}, and #{array[2]}"
  else
    list = ""
    
  end
end